# Installed Plugins

This directory contains compiled plugin files (.dart compiled or packaged).

Plugins should be placed here after compilation. The plugin loader will scan this directory on server startup.

## Plugin Format

Plugins should be compiled Dart packages or packaged in a format that the plugin loader can understand.

