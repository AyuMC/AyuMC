# Cache Directory

This directory contains temporary cache files:

- Chunk cache
- Resource cache
- Plugin cache
- Other temporary files

This directory can be safely cleared, but it may cause slower startup times as caches are rebuilt.

