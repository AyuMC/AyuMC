# Server Configuration

This directory contains server configuration files:

- **server.properties**: Main server configuration
- **bukkit.yml**: Bukkit configuration (if needed)
- **spigot.yml**: Spigot configuration (if needed)
- **ayumc.yml**: Custom AyuMC configuration

Configuration files will be generated automatically when the server starts for the first time.
