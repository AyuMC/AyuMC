name: ayumc
description: A sample command-line application with basic argument parsing.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.10.4

dependencies:
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
